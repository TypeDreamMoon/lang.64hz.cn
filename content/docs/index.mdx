---
title: DreamShaderLang
description: 为了代替虚幻引擎材质“连连看”而生的文本化材质语言。
---

一句话来说，DreamShaderLang 是为了代替虚幻引擎材质“连连看”而生的语言。

DreamShaderLang 是 DreamShader 插件的源语言。它用 `.dsm` 和 `.dsh` 文件描述 Unreal 材质、材质函数、共享 helper、包依赖和材质图连接关系，然后由插件生成标准 `UMaterial` 与 `UMaterialFunction` 资产。

这套文档以语言为中心组织，目标是让 DreamShaderLang 像常见编程语言一样可学习、可查询、可引用。

## 核心模型

| 概念 | 作用 |
| --- | --- |
| `.dsm` | Dream Shader Material。生成材质或材质函数的主文件。 |
| `.dsh` | Dream Shader Header。存放共享 `Function`、`Namespace` 与 import。 |
| `Shader` | 顶层材质声明，生成 Unreal `UMaterial`。 |
| `ShaderFunction` | 顶层材质函数声明，生成 Unreal `UMaterialFunction`。 |
| `VirtualFunction` | 声明已有 Unreal `UMaterialFunction`，供 `Graph` 调用但不生成或覆盖资产。 |
| `Graph` | 生成 Unreal 材质节点的图 DSL。 |
| `Function` | HLSL 风格 helper，供 `Graph` 调用。 |
| `Namespace` | 组织共享 helper，调用形式为 `Namespace::Function(...)`。 |
| `Package` | 可分发的 `.dsh` 共享库，安装到 `DShader/Packages`。 |

## 公开仓库

| 项目 | 链接 |
| --- | --- |
| DreamShader 插件 | [TypeDreamMoon/DreamShader](https://github.com/TypeDreamMoon/DreamShader) |
| VSCode 扩展 | [TypeDreamMoon/dreamshader-language-support](https://github.com/TypeDreamMoon/dreamshader-language-support) |
| Package Store Index | [TypeDreamMoon/dreamshader-package-index](https://github.com/TypeDreamMoon/dreamshader-package-index) |

## 语言分层

DreamShaderLang 不是把 HLSL 直接塞进材质文件。它分成三层：

| 层级 | 写在哪里 | 适合表达 |
| --- | --- | --- |
| 资产层 | `Shader` / `ShaderFunction` / `VirtualFunction` | Unreal 资产名称、属性、设置、输出绑定，或现有材质函数签名。 |
| 图层 | `Graph = { ... }` | 变量、节点、表达式、`UE.*`、材质函数调用、基础 `if` / `else`。 |
| Helper 层 | `Function` / `Namespace` | 可复用 HLSL 计算、循环、复杂流程、自包含 Custom 节点代码。 |

## 最小材质

```c
Shader(Name="DreamMaterials/M_Minimal")
{
    Properties = {
        vec3 Tint = vec3(1.0, 0.2, 0.2);
    }

    Settings = {
        Domain = "UI";
        ShadingModel = "Unlit";
    }

    Outputs = {
        vec3 Color;
        Base.EmissiveColor = Color;
    }

    Graph = {
        Color = Tint;
    }
}
```

## 推荐阅读顺序

| 目标 | 页面 |
| --- | --- |
| 第一次写材质 | [安装与准备](./getting-started/installation.mdx)、[第一个材质](./getting-started/first-material.mdx) |
| 理解项目结构 | [项目布局](./getting-started/project-layout.mdx)、[文件模型](./syntax/file-model.mdx) |
| 学语言核心 | [顶层声明](./syntax/declarations.mdx)、[Section](./syntax/sections.mdx)、[类型和值](./syntax/types-and-values.mdx) |
| 写共享逻辑 | [Function 与 Namespace](./syntax/functions.mdx)、[Import 与 Namespace](./syntax/imports-and-namespaces.mdx) |
| 写材质图 | [Graph 语言](./syntax/graph.mdx)、[UE 节点入口参考](./reference/ue-nodes.mdx) |
| 查资产输出 | [材质设置](./reference/material-settings.mdx)、[输出绑定](./reference/outputs.mdx)、[Package 参考](./reference/packages.mdx) |
| 看示例 | [常用模式](./examples/patterns.mdx)、[配方示例](./examples/recipes.mdx) |
| 排错与生成 | [资产生成](./compiler/asset-generation.mdx)、[诊断](./compiler/diagnostics.mdx)、[限制](./compiler/limitations.mdx) |
| 了解插件和工具 | [DreamShader 插件](./dreamshader/overview.mdx)、[VSCode 扩展](./workflows/vscode.mdx)、[Package 作者工作流](./workflows/package-authoring.mdx) |
| 追踪版本变化 | [ChangeLog](./changelog/index.mdx)、[插件更新](./changelog/dreamshader.mdx)、[VSCode 更新](./changelog/vscode-extension.mdx)、[网站更新](./changelog/website.mdx) |
| 补充着色语言背景 | [HLSL 简介](./shader-languages/hlsl.mdx)、[GLSL 简介](./shader-languages/glsl.mdx)、[关键字速查](./shader-languages/keywords-cheatsheet.mdx) |

## 当前版本

| 项目 | 内容 |
| --- | --- |
| DreamShader 插件 | `1.2.3` |
| VSCode 扩展 | `1.2.22` |
| 文档站 | `lang.64hz.cn` |
| 语言 | `DreamShaderLang` |
| 文件扩展名 | `.dsm` / `.dsh` |
| 作者 | TypeDreamMoon |
| 主要产物 | `UMaterial` / `UMaterialFunction` |

## 1.2.3 重点

| 能力 | 说明 |
| --- | --- |
| 参数元数据 | `Properties`、`ShaderFunction` / `VirtualFunction` pin 支持 `[Group="...", SortPriority=32, Description="..."]`。 |
| 显式 Parameter 节点 | `ScalarParameter`、`VectorParameter`、`TextureObjectParameter`、`TextureSampleParameter2D`、`StaticBoolParameter`、`StaticSwitchParameter` 等可直接写在 `Properties`。 |
| 静态开关 | `StaticSwitchParameter UseDetail = true;` 可在 `Graph` 中用 `UseDetail(True=..., False=...)` 选择分支。 |
| MPC 参数 | `UE.CollectionParam(Collection=Path(...), Parameter="Name")` 读取 Material Parameter Collection。 |
| 可选函数输入 | `ShaderFunction` / `VirtualFunction` 输入可写 `opt`，调用侧可传 `default` 或省略尾部可选参数。 |
